# 딸랑구 UI 개편 및 신규 화면 작업 명세
## VS Code + Codex 협업용 Markdown

> 기준 문서: `UI_modplan.pdf`  
> 목적: Flutter 프로젝트에서 Codex와 화면 구현·수정 작업을 진행하기 위한 실행 가능한 작업 명세  
> 원칙: 이 문서의 **확정 요구사항**은 PDF 내용을 기준으로 하며, PDF에서 정해지지 않은 사항은 임의로 확정하지 않고 `확인 필요`로 남긴다.

---

## 0. Codex 작업 원칙

Codex는 코드를 수정하기 전에 반드시 현재 저장소를 먼저 분석한다.

### 먼저 확인할 항목

1. Flutter 프로젝트 루트와 실행 방법
2. 현재 라우팅 방식
   - `Navigator`
   - `go_router`
   - `auto_route`
   - 기타
3. 기존 화면 파일 경로
4. 기존 음성 인터랙션 진입점
5. 기존 스몰톡 또는 말풍선 템플릿
6. 딸랑구 캐릭터 asset 경로
7. 현재 색상·텍스트·버튼 디자인 토큰
8. 사용자 이름이 저장·전달되는 방식
9. Accessibility Service와 Flutter 사이의 현재 연결 구조
10. 기존 테스트 코드와 lint 규칙

### Codex가 임의로 하면 안 되는 것

- 실제 라우트명을 추측해서 새로 만들지 않는다.
- 기존에 사용자 이름을 전달하는 방식이 있으면 별도 전역 변수를 만들지 않는다.
- 기존 캐릭터 asset이 있는데 placeholder로 교체하지 않는다.
- Accessibility Service의 실제 자동화 로직을 Flutter 작업에 포함하지 않는다.
- 문서에서 결정되지 않은 화면 순서나 스타일 충돌을 임의로 확정하지 않는다.
- 기존 앱 아키텍처를 확인하지 않고 상태 관리 라이브러리를 새로 도입하지 않는다.
- 폰트 크기를 줄여서 줄바꿈 문제를 해결하지 않는다.

### 코드 수정 전 보고 형식

Codex는 첫 분석 후 다음 내용을 요약한다.

```md
## 저장소 분석 결과

- 라우팅 방식:
- 홈 화면 경로:
- 음성 인터랙션 진입점:
- 스몰톡 화면/템플릿 경로:
- 사용자 이름 상태 위치:
- 캐릭터 asset 경로:
- 공통 색상/텍스트 테마:
- Accessibility 연동 관련 파일:
- 이번 작업에서 수정할 예상 파일:
- 새로 만들 예상 파일:
- 구현 전 확인이 필요한 사항:
```

---

# 1. 전체 목표

딸랑구 앱을 고령층이 쉽게 사용할 수 있는 음성 기반 쇼핑 앱으로 개편한다.

핵심 목표는 다음과 같다.

- 앱 최초 진입 흐름 추가
- 온보딩 및 본인확인 화면 추가
- 홈 화면 추가
- 음성 쇼핑 프로세스 UI 개선
- 실제 결제 자동화 시점의 하단 오버레이 UI 추가
- 쇼핑 완료 화면 추가
- 화면 간 디자인 일관성 확보
- 큰 글씨, 큰 터치 영역, 높은 대비 유지
- 공통 컴포넌트 재사용
- 동적 텍스트와 사용자 이름 하드코딩 제거

---

# 2. 전체 사용자 흐름

## 문서 내용을 기준으로 한 예상 흐름

```text
앱 실행
  ↓
스플래시
  ↓
온보딩 3페이지
  ↓
본인확인/이름 질문
  ↓
사용 쇼핑 플랫폼 확인
  ↓
스몰톡
  ↓
메인 홈
  ↓
“딸랑구야 도와줘!”
  ↓
상품 요청
  ↓
상품 추천 확인
  ↓
수량 확인
  ↓
장바구니 담기 진행
  ↓
장바구니 확인
  ↓
배송지 확인
  ↓
비밀번호 입력
  ↓
Accessibility Service 실제 자동화
  ↓
쇼핑 완료
  ↓
홈으로 복귀
```

## 아직 확정되지 않은 흐름

다음 항목은 PDF에서 최종 확정되지 않았다.

- 온보딩 `시작하기` 이후 바로 홈으로 갈지, 본인확인으로 갈지
- 플랫폼 확인 화면이 본인확인/스몰톡 전인지 후인지
- 온보딩을 매번 표시할지 최초 1회만 표시할지

Codex는 기존 코드와 현재 기획 의도를 확인한 뒤, 임의 구현 전에 사용자에게 질문한다.

---

# 3. 구현 범위

## Flutter 담당 범위

- 화면 UI
- 재사용 컴포넌트
- 화면 간 네비게이션
- 상태에 따른 텍스트 및 색상 변경
- 페이드, 슬라이드, 축소·확장 전환 애니메이션
- 외부에서 값을 주입받는 인터페이스
- 취소·완료 콜백 인터페이스
- 접근성 대응
- 기존 음성 인터랙션 진입점 연결

## Flutter 담당 범위가 아닌 항목

- 실제 Accessibility Service 자동화
- 실제 쇼핑 플랫폼 앱 조작
- Android 시스템 오버레이 윈도우 크기 조절
- 실제 결제 중단 로직
- 자동화 완료 신호 발생 시점 결정
- 구매이력 데이터 수집 방식 결정

---

# 4. 공통 디자인 원칙

## 4.1 고령층 접근성

- 큰 글씨를 유지한다.
- 텍스트가 잘리면 폰트 크기를 줄이지 않고 컨테이너 높이를 늘린다.
- 문장은 어절 중간이 아니라 자연스러운 문장 단위로 줄바꿈한다.
- 버튼과 칩의 터치 영역을 충분히 크게 유지한다.
- 텍스트와 배경 사이 명도 대비를 명확히 확보한다.
- 활성 상태와 비활성 상태가 색상만 보아도 구분되어야 한다.
- 이미지 위 UI는 그라데이션이나 틴트로 대비를 확보한다.
- 중요한 정보는 시각적 우선순위가 명확해야 한다.

## 4.2 색상 사용

핑크색은 강조 용도로 제한한다.

사용 권장 위치:

- 사용자 이름
- 강조 단어
- 활성 마이크
- 현재 진행 단계
- 주요 CTA
- 완료 체크
- 핵심 상태

비활성 요소는 회색을 사용한다.

## 4.3 공통 레이아웃

- 스테퍼의 원과 선을 정확히 중앙 정렬한다.
- 화면 간 동일 기능 버튼의 크기를 통일한다.
- 하단 버튼은 SafeArea를 고려한다.
- 스크롤 영역은 마이크와 하단 버튼을 침범하지 않는 범위에서 최대화한다.
- 긴 텍스트는 `Flexible`, `Expanded`, 유동 높이 컨테이너를 우선 검토한다.

---

# 5. 공통 컴포넌트 후보

Codex는 기존 코드에 유사 컴포넌트가 있는지 먼저 확인한다.  
기존 컴포넌트가 있다면 새로 만들지 말고 확장한다.

## 5.1 `ShoppingProgressStepper`

### 사용 화면

- 상품 요청
- 상품 추천
- 수량 확인
- 장바구니 진행
- 장바구니 확인
- 배송지 확인
- 쇼핑 완료

### 요구 기능

```dart
ShoppingProgressStepper(
  currentStep: ShoppingStep.productCheck,
  completedSteps: {...},
  showCompletedCheck: true,
)
```

### 단계

```dart
enum ShoppingStep {
  productCheck,
  productSelection,
  addToCart,
  payment,
}
```

### 시각 요구

- 원 크기 확대
- 연결선 길이 축소
- 원과 선 중앙 정렬
- 비활성 회색
- 활성 핑크
- 완료 상태 체크 아이콘 지원
- 완료 화면에서는 네 단계 모두 체크 표시

---

## 5.2 `DialogueBubble`

### 요구 기능

- 외부 텍스트 주입
- 특정 단어 또는 구간 강조
- 유동 높이
- 큰 글씨
- 자연스러운 줄바꿈
- 텍스트 변경 시 페이드 전환
- 기존 말풍선 스타일 재사용

### 권장 인터페이스 예시

```dart
class DialogueSegment {
  final String text;
  final bool emphasized;
}

DialogueBubble(
  segments: const [
    DialogueSegment(text: '성함', emphasized: true),
    DialogueSegment(text: '이 어떻게 되세요?'),
  ],
)
```

또는 기존 구조에 맞춰 다음 방식도 가능하다.

```dart
DialogueBubble(
  text: '성함이 어떻게 되세요?',
  highlightedWords: const ['성함'],
)
```

Codex는 기존 코드에 더 잘 맞는 방식을 선택하되, 선택 이유를 보고한다.

---

## 5.3 `VoiceInputButton`

### 상태

```dart
enum VoiceInputState {
  inactive,
  active,
  listening,
}
```

### 요구사항

- 비활성: 원과 텍스트 회색
- 활성: 채도 높은 핑크
- 기존 파동 또는 펄스 애니메이션 유지
- 버튼 크기 및 터치 영역 유지
- 상태에 따라 색상만 명확히 변경

---

## 5.4 `EndConversationButton`

### 공통 기능

- 모든 화면에서 동일한 종료 콜백 제공
- 최소 터치 영역 확보
- 접근성 라벨 제공

### 스타일 충돌

PDF 안에 두 가지 지시가 존재한다.

#### 스타일 A

- 흰색 배경
- 회색 테두리
- 진한 회색 텍스트
- 큰 글씨

#### 스타일 B

- 검정 배경
- 채도 높은 핑크 텍스트

Codex는 임의로 하나를 전체 공통 스타일로 확정하지 않는다.

권장 처리:

```dart
enum EndConversationButtonVariant {
  light,
  dark,
}
```

최종 디자인 확정 전에는 화면별 PDF 지시를 따르거나, 사용자에게 통일 기준을 질문한다.

---

## 5.5 `CharacterView`

지원해야 할 형태:

- 전신 캐릭터
- 손 흔드는 캐릭터
- 얼굴 원형 이미지
- 임시 placeholder

Codex는 GitHub에 push된 실제 asset을 먼저 검색한다.

---

## 5.6 `BottomStatusBanner`

### 사용 화면

- 쇼핑 플랫폼 확인
- Accessibility 자동화

### 요구 기능

- 하단에서 슬라이드 인
- 상태 텍스트 외부 주입
- 캐릭터 표시
- 종료 버튼 또는 종료 아이콘
- 상단 경계선 또는 그림자
- 외부 콜백

---

## 5.7 `ProductCard`

### 요구 기능

- 플랫폼명
- 상품 이미지
- 상품명
- 가격
- 옵션
- 수량
- 테두리 또는 옅은 배경
- 이미지 위 그라데이션 오버레이 옵션

---

# 6. 데이터 및 상태

기존 상태 관리 방식을 유지한다.

## 6.1 사용자 상태

```dart
class UserProfile {
  final String name;
}
```

필요 값:

- 사용자 이름
- 온보딩 완료 여부
- 확인된 쇼핑 플랫폼 목록

## 6.2 대화 상태

필요 값:

- 현재 대사
- 강조 구간
- Agent 생성 텍스트
- 마이크 상태
- 음성 인식 결과

## 6.3 쇼핑 상태

필요 값:

- 요청 상품
- 추천 상품
- 선택 수량
- 장바구니 항목
- 배송지
- 배송 요청사항
- 주문자
- 전화번호

## 6.4 자동화 상태

권장 상태 예시:

```dart
enum AutomationUiState {
  idle,
  preparing,
  running,
  completed,
  cancelled,
  failed,
}
```

필요 값:

- 상태 텍스트
- 자동화 진행 여부
- 완료 여부

실제 Accessibility Service 상태 모델이 이미 있다면 그것을 재사용한다.

---

# 7. 화면별 작업 명세

---

## 화면 1. 스플래시

### 작업 유형

기존 화면 수정

### 확정 요구사항

- 하단 로딩 스피너 삭제
- 스피너가 실제 로딩 상태 표현에 필수라면 삭제 전에 이유 보고
- 기존 로고, 캐릭터, 배경 유지
- 수정 후 앱 실행 확인

### Codex 확인 항목

- 스플래시에서 수행하는 비동기 초기화 작업
- 스플래시 최소 노출 시간
- 로그인 상태 확인 여부
- 온보딩 완료 여부 확인 위치

### 완료 조건

- 화면 하단에 스피너가 보이지 않는다.
- 초기화 완료 후 다음 화면으로 정상 이동한다.
- 빈 공간이 부자연스럽지 않다.
- 앱이 정지된 것처럼 오래 보이지 않는다.

---

## 화면 2. 온보딩 3페이지

### 작업 유형

신규 화면

### 위치

스플래시 다음, 로그인/메인 화면 진입 전

단, 정확한 다음 화면은 확인 필요.

### 공통 구조

- 총 3페이지
- 좌우 스와이프
- 하단 페이지 인디케이터
- 현재 페이지 도트는 진하거나 길게 표시
- 1·2페이지 버튼 없음
- 3페이지에만 `시작하기`
- 핑크 계열 테마

### 페이지 1

```text
전화하듯 말만 하면 돼요
딸기 사줘
```

### 페이지 2

```text
복잡한 결제도 걱정 마세요
주문 완료
결제 승인
```

### 페이지 3

```text
이제 딸랑구와 함께 편하게 쇼핑해보세요
```

버튼:

```text
시작하기
```

### 이미지

- 기존 딸랑구 asset 우선 사용
- asset이 없으면 placeholder
- 딸기, 장바구니 배경 일러스트는 1차 구현에서 생략 가능

### 확인 필요

- 매번 표시
- 최초 1회만 표시

### 최초 1회 방식일 경우

기존 로컬 저장소가 있으면 재사용한다.  
없다면 프로젝트 관례에 맞는 최소 구현을 사용한다.

예시 키:

```text
hasCompletedOnboarding
```

### 완료 조건

- 3페이지가 좌우 스와이프로 이동한다.
- 인디케이터가 현재 페이지와 동기화된다.
- 1·2페이지에는 시작 버튼이 없다.
- 3페이지에만 시작 버튼이 있다.
- 시작 버튼이 확정된 다음 라우트로 이동한다.
- 큰 글씨와 충분한 버튼 영역을 유지한다.

---

## 화면 3. 본인확인/스몰톡

### 작업 유형

신규 화면

### 상태 1

```text
안녕하세요! 저는 딸랑구예요 :)
오늘도 반갑게 인사하러 왔어요!
```

### 상태 2

```text
성함이 어떻게 되세요?
```

강조:

```text
성함
```

### 요구사항

- 말풍선, 캐릭터, 마이크 버튼 레이아웃 동일
- 대사 텍스트만 상태에 따라 변경
- 텍스트 하드코딩 금지
- 특정 단어를 외부 파라미터로 강조 가능
- 대사 전환 시 페이드 애니메이션
- 큰 글씨, 높은 대비

### 완료 조건

- 외부 상태 변경만으로 대사가 교체된다.
- 강조 단어가 핑크로 표시된다.
- 텍스트 전환이 갑자기 바뀌지 않고 페이드된다.
- 레이아웃이 전환 중 흔들리지 않는다.
- 이름 입력 또는 음성 인식 결과가 기존 상태 구조에 저장된다.

---

## 화면 4. 쇼핑 플랫폼 확인 배너

### 작업 유형

신규 화면 또는 신규 오버레이 컴포넌트

### 화면 성격

사용자 폰에 설치된 쇼핑 플랫폼을 확인하는 동안 표시한다.

### 배경

- 실제 시스템 홈 화면 위 오버레이라면 Flutter 배경 구현 불필요
- 앱 내부 목업이면 홈 화면 스크린샷 또는 placeholder 배경 사용

### 하단 배너 구성

왼쪽:

- 원형 딸랑구 얼굴

가운데:

```text
[이름]님이 사용중인 쇼핑 플랫폼을 확인하고 있어요!
```

오른쪽:

- 핑크 원형 배경
- 흰색 X
- `대화 종료`

### 요구사항

- 이름 외부 주입
- 화면 진입 시 하단에서 위로 슬라이드
- 전체 풀스크린이 아니라 하단 배너
- 큰 글씨, 높은 대비

### 확인 필요

- 본인확인 전 또는 후
- 실제 플랫폼 스캔 결과를 누가 전달하는지
- 구매이력 확인 기능이 이 화면 범위에 포함되는지

### 완료 조건

- 배너가 하단에서 자연스럽게 등장한다.
- 사용자 이름이 동적으로 표시된다.
- X 버튼이 외부 종료 콜백을 호출한다.
- 홈 화면 위 또는 지정 배경 위에서 UI가 잘 보인다.

---

## 화면 5. 일반 스몰톡

### 작업 유형

기존 템플릿 재사용

### 요구사항

- `52p 만들어둔 템플릿` 그대로 사용
- Agent가 생성한 텍스트 표시
- 신규 레이아웃을 임의로 만들지 않음

### Codex 확인 항목

- `52p 템플릿`이 의미하는 파일
- 기존 말풍선 또는 스몰톡 화면
- Agent 응답 전달 방식
- 스트리밍 여부

### 완료 조건

- 기존 템플릿을 재사용한다.
- Agent 텍스트가 외부 상태로 표시된다.
- 긴 문장에서도 잘리지 않는다.

---

## 화면 6. 메인 홈

### 작업 유형

신규 화면

### UI 구성

1. 상단 홈 아이콘
2. 딸랑구 대사 말풍선
3. 손 흔드는 캐릭터
4. 바로가기 카드 2개
5. 하단 핵심 CTA

### 상단 홈 아이콘

- 현재 홈 표시
- 눌러도 별도 동작 없음
- 비활성 회색 가능

### 말풍선

```text
안녕하세요! 저는 딸랑구예요 :)
오늘도 반갑게 인사하러 왔어요!
```

기존 `DialogueBubble` 재사용

### 바로가기 카드

#### 카드 1

```text
장바구니 보기
```

- 장바구니 아이콘
- 핑크 배경
- 백엔드 연결 없음
- `TODO` 콜백

#### 카드 2

```text
지난 주문 내역
```

- 쇼핑백 아이콘
- 노란 배경
- 백엔드 연결 없음
- `TODO` 콜백

### 메인 CTA

```text
딸랑구야 도와줘!
```

- 전화 아이콘
- 핑크 그라데이션
- 화면 하단 고정
- 실제 음성 인터랙션 진입점으로 이동

### 최우선 요구사항

`딸랑구야 도와줘!` 버튼 네비게이션은 실제로 동작해야 한다.

### Codex 확인 항목

- 현재 음성 인터랙션 라우트
- 기존 메인 프로세스 시작 함수
- route stack 처리 방식

### 완료 조건

- 홈 화면 UI가 표시된다.
- 카드 두 개는 시각적으로 존재한다.
- 카드 클릭 로직은 TODO로 남는다.
- CTA가 기존 음성 인터랙션 화면으로 정상 이동한다.
- 하단 CTA가 작은 화면에서도 잘리지 않는다.

---

## 화면 7. 상품 요청 음성 화면

### 작업 유형

기존 화면 수정

### 상단 스테퍼

- 원을 조금 더 크게
- 연결선을 조금 더 짧게
- 선과 원 중앙 정렬
- 비활성 회색
- 활성 진한 핑크
- 기존 애니메이션 유지

### 인사 텍스트

기존:

```text
김영희님, 오늘은 무엇을 구매하고 싶으신가요?
```

변경:

```text
김영희님
뭐가 필요하세요?
```

요구사항:

- 두 줄
- 행간 축소
- 사용자 이름 핑크 강조
- 사용자 이름 하드코딩 금지
- 앞 단계에서 받은 이름 사용

### 마이크 버튼

비활성:

- 원 회색
- 글씨 회색

활성:

- 채도 높은 핑크
- 기존 파동/펄스 유지

### 예시 칩

남길 항목:

```text
토마토 사고 싶어
삼겹살 1근 구매해줘
```

삭제:

```text
감귤 2박스 담아줘
```

키보드 아이콘 유지

### 대화 종료 버튼

이 화면의 PDF 지시:

- 흰색 배경
- 회색 테두리
- 진한 회색 텍스트
- 글씨 크게

### 완료 조건

- 스테퍼 정렬이 맞다.
- 사용자 이름이 동적으로 표시된다.
- 문구가 정확히 두 줄로 보인다.
- 마이크 상태별 색상이 분명하다.
- 예시 칩은 두 개만 남는다.
- 키보드 아이콘이 유지된다.
- 종료 버튼 대비가 충분하다.

---

## 화면 8. 상품 추천 확인

### 작업 유형

기존 화면 수정

### 상단 문장

예시:

```text
컬리에서 고당도 수박 1kg를
```

요구사항:

- 어색한 지점에서 줄바꿈 금지
- 한 줄 또는 자연스러운 구간에서 줄바꿈
- 박스 높이 유동 확장
- 폰트 크기 축소 금지

### 상품 이미지 카드

- 테두리 또는 배경 효과
- 명확한 카드 형태
- 기존 정보 유지

### 대화 종료 버튼

PDF 지시:

- 이전 화면과 크기 동일
- 검정 배경
- 채도 높은 핑크 텍스트

> 화면 7과 스타일 지시가 충돌하므로 최종 통일 기준 확인 필요

### 완료 조건

- 문장이 어절 중간에서 잘리지 않는다.
- 상품 이미지가 카드로 명확히 구분된다.
- 버튼 크기가 이전 화면과 동일하다.
- 작은 화면에서도 내용이 겹치지 않는다.

---

## 화면 9. 수량 확인

### 작업 유형

기존 화면 수정

### 텍스트

변경:

```text
좋아요
몇 개 담아드릴까요?
```

강조:

```text
몇 개
```

### 이미지 위 UI

문제:

- 상품 이미지 채도가 높아 마이크 UI가 잘 보이지 않음

요구사항:

- 이미지 전체 blur 금지
- 하단에서 위로 반투명 그라데이션 권장
- 필요 시 tint 적용
- 상품은 계속 식별 가능해야 함
- 마이크 버튼과 문구가 분명히 보이게 함

### 완료 조건

- 텍스트가 지정된 두 줄로 표시된다.
- `몇 개`만 핑크로 강조된다.
- 이미지 위 마이크 UI의 대비가 충분하다.
- 상품 이미지 자체는 알아볼 수 있다.
- 기존 스테퍼와 버튼 스타일을 유지한다.

---

## 화면 10. 장바구니 담기 진행

### 작업 유형

기존 화면 수정

### 제목

```text
장바구니 작업
```

- 글씨 크기 축소
- 하위 라벨 수준의 시각적 우선순위

### 설명 텍스트

```text
고당도 수박을 장바구니에 담고 있어요.
옵션과 수량을 확인한 뒤 주문서에 반영하고 있어요.
```

- 문장 단위 줄바꿈
- 어절 중간 분리 금지
- 폰트 크기 유지

### 담은 상품명

```text
[컬리] 고당도 수박 2개
```

- 글씨 크기 확대
- 핵심 정보 강조

### 진행 바

- 그림자/블러 제거
- 회색 트랙
- 핑크 진행 영역
- 과도한 glow 금지

### 완료 조건

- 제목이 본문보다 작다.
- 설명 문장이 자연스럽게 보인다.
- 상품명이 눈에 잘 띈다.
- 진행 바가 회색 트랙과 핑크 진행 영역으로 구성된다.

---

## 화면 11. 장바구니 확인

### 작업 유형

기존 화면 수정

### 상단 안내

```text
장바구니에 담았어요.
더 구매하시겠어요, 아니면 결제할까요?
```

요구사항:

- 자연스러운 줄바꿈
- 텍스트 박스를 위로 이동
- 과도한 상단 여백 축소

### 장바구니 목록

제목 예시:

```text
김영희 님의 장바구니
```

요구사항:

- 기존 스크롤 로직 유지
- 보이는 목록 영역 높이 확대
- 하단 마이크와 종료 버튼 침범 금지
- 사용자 이름 동적 표시

### 완료 조건

- 안내 문구가 자연스럽게 나뉜다.
- 상단 여백이 줄어든다.
- 장바구니 목록이 기존보다 훨씬 넓게 보인다.
- 목록 스크롤이 유지된다.
- 하단 요소가 가려지지 않는다.

---

## 화면 12. 배송지 확인

### 작업 유형

기존 화면 수정

### 상단 문구

```text
배송지 맞으세요?
```

- 검정색
- 이전 질문 화면과 같은 형식

### 배송지 카드

- 기존 그림자 제거
- 옅은 회색 배경 또는 회색 테두리
- 검정 텍스트 대비 유지
- 텍스트 크기 전반 확대

대상:

- 주소
- 배송 요청사항
- 주문자
- 전화번호

### 완료 조건

- 상단 질문이 이전 화면과 같은 위치·스타일로 보인다.
- 카드가 회색 톤으로 구분된다.
- 주소와 사용자 정보가 큰 글씨로 표시된다.
- 긴 주소가 잘리지 않는다.

---

## 화면 13. 비밀번호 입력

### 작업 유형

기존 화면 수정

### 키패드 영역

- 전체 카드 배경을 그레이 톤으로 변경
- 숫자 버튼 자체 색상은 유지

### 숫자

- 0~9 글씨 크기 확대
- 터치 영역 축소 금지

### 완료 조건

- 키패드 외곽 영역이 회색이다.
- 숫자가 기존보다 크게 보인다.
- 숫자 버튼 배치가 깨지지 않는다.
- 비밀번호 입력 기능이 기존대로 동작한다.

---

## 화면 14. Accessibility 자동화 오버레이

### 작업 유형

신규 컴포넌트

### 동작 시점

다음 과정이 모두 끝난 뒤 표시한다.

```text
결제 확정
→ 배송지 확인
→ 비밀번호 입력
→ 실제 자동화 시작
```

### 배경

- 실제 쇼핑 플랫폼 앱 화면
- Flutter에서는 하단 배너 UI와 애니메이션만 담당

### 배너 크기

- 화면 하단 약 1/4

### 상태 문구

```text
결제해볼게요!
```

현재 문구는 하나지만 외부 String 파라미터로 구현한다.

### 배너 UI

- 상태 텍스트
- 대화 종료 버튼
- 상단 경계선 또는 그림자
- 기존 디자인과 일관된 큰 글씨와 높은 대비

### 애니메이션

자동화 시작:

- 풀스크린 비밀번호 화면에서 하단 배너 상태로 축소
- 아래로 접히는 느낌
- 0.3~0.5초
- ease-in-out

자동화 완료:

- 배너에서 풀스크린 완료 화면으로 확장
- 완료 신호 이후 화면 15로 이동

### 콜백

```dart
class AutomationOverlayBanner extends StatelessWidget {
  final String statusText;
  final VoidCallback? onCancelPressed;
  final VoidCallback? onAutomationComplete;
}
```

실제 코드 인터페이스는 프로젝트 구조에 맞춘다.

### 범위 제외

- 실제 자동화 중단
- 실제 Accessibility 동작
- 시스템 오버레이 창 크기 변경
- 자동화 완료 신호 발생 로직

### 완료 조건

- 상태 문구가 외부에서 주입된다.
- 취소 버튼이 콜백을 호출한다.
- 자동화 완료 콜백 인터페이스가 열려 있다.
- 배너가 하단 UI로 표시된다.
- 지정된 전환 애니메이션이 적용된다.

---

## 화면 15. 쇼핑 완료

### 작업 유형

신규 화면

### 스테퍼

4단계 모두 완료 체크:

```text
상품 확인 ✓
상품 고르기 ✓
장바구니 담기 ✓
결제하기 ✓
```

### 메시지

제목:

```text
쇼핑 완료!
```

설명:

```text
주문이 성공적으로 완료되었어요.
이제 안심하고 기다리시면 돼요.
```

### 완료 아이콘

- 원형 핑크 배경
- 중앙 체크
- 주변 작은 별 또는 반짝임
- 과도하지 않게

### 하단 버튼

```text
홈 화면으로 돌아가기
```

- 실제 홈 라우트 연결
- 쇼핑 프로세스 route stack 정리 검토
- 뒤로가기 시 결제 화면으로 돌아가지 않게 처리 권장

### 상태 관리

- 정적 완료 화면
- 현재 단계에서는 화면 자체 우선 구현
- 자동화 완료 신호 연결은 추후 가능
- 필요 시 `onComplete` 인터페이스 유지

### 완료 조건

- 스테퍼 네 단계가 모두 완료 상태다.
- 메시지가 자연스럽게 두 문장으로 보인다.
- 완료 체크 아이콘이 표시된다.
- 홈 버튼이 실제 홈으로 이동한다.
- 뒤로가기 스택이 비정상적으로 남지 않는다.

---

# 8. 네비게이션 요구사항

Codex는 기존 라우팅 방식을 유지한다.

## 반드시 연결해야 하는 경로

1. 스플래시 → 다음 진입 화면
2. 온보딩 마지막 → 확정된 다음 화면
3. 본인확인 → 다음 화면
4. 홈 CTA → 기존 음성 쇼핑 진입점
5. 쇼핑 단계별 다음 화면
6. 비밀번호 완료 → 자동화 배너
7. 자동화 완료 → 쇼핑 완료
8. 쇼핑 완료 → 홈

## route stack 주의

- 쇼핑 완료 후 홈으로 돌아갈 때 이전 결제 화면이 뒤로가기 스택에 남지 않도록 한다.
- 기존 프로젝트의 `pushReplacement`, `go`, `popUntil` 등 관례를 따른다.
- 임의로 새로운 네비게이션 패턴을 혼용하지 않는다.

---

# 9. 애니메이션 요구사항

## 9.1 대사 변경

- 페이드 전환
- 말풍선 크기가 갑자기 튀지 않도록 처리
- 권장 시간: 기존 앱 기준에 맞춤
- 문서에는 정확한 시간 미지정

## 9.2 플랫폼 확인 배너

- 하단에서 위로 슬라이드
- 전체 화면이 아닌 배너 자체만 이동

## 9.3 자동화 배너

- 0.3~0.5초
- ease-in-out
- 풀스크린에서 하단으로 접히는 느낌
- 완료 시 다시 확장

## 9.4 마이크

- 기존 파동/펄스 애니메이션 유지
- 색상만 상태별로 변경

---

# 10. 접근성 및 품질 체크리스트

## 텍스트

- [ ] 폰트 크기를 줄여 문제를 회피하지 않았다.
- [ ] 긴 문장이 어절 중간에서 잘리지 않는다.
- [ ] 강조 단어와 일반 문장 대비가 명확하다.
- [ ] 텍스트가 시스템 글자 크기 확대에서 깨지지 않는다.
- [ ] 사용자 이름이 길어도 overflow가 발생하지 않는다.

## 버튼

- [ ] 주요 버튼의 터치 영역이 충분하다.
- [ ] 하단 버튼이 SafeArea와 겹치지 않는다.
- [ ] 활성/비활성 상태가 명확하다.
- [ ] 아이콘에 semantic label이 있다.
- [ ] 대화 종료 버튼 대비가 충분하다.

## 이미지

- [ ] 캐릭터가 화면 크기에 따라 잘리지 않는다.
- [ ] 상품 이미지 위 UI가 분명히 보인다.
- [ ] 상품 이미지는 여전히 식별 가능하다.
- [ ] placeholder는 실제 asset이 없을 때만 사용한다.

## 스크롤

- [ ] 장바구니 영역이 충분히 넓다.
- [ ] 키보드 또는 하단 버튼 때문에 내용이 가려지지 않는다.
- [ ] 작은 화면에서 overflow가 없다.

## 네비게이션

- [ ] 홈 CTA가 실제 음성 인터랙션으로 이동한다.
- [ ] 완료 화면에서 홈으로 정상 이동한다.
- [ ] 뒤로가기 스택이 비정상적으로 남지 않는다.
- [ ] 온보딩 표시 정책이 반영되어 있다.

---

# 11. 테스트 요구사항

## 위젯 테스트 권장 대상

- `ShoppingProgressStepper`
- `DialogueBubble`
- `VoiceInputButton`
- `EndConversationButton`
- `BottomStatusBanner`
- 온보딩 페이지 버튼 노출 조건
- 사용자 이름 동적 표시
- 강조 단어 렌더링
- 마이크 활성/비활성 색상
- 쇼핑 완료 홈 이동

## 수동 테스트 화면 크기

최소 다음 조건에서 확인한다.

- 작은 Android 화면
- 일반 Android 화면
- 큰 글자 설정
- 긴 사용자 이름
- 긴 상품명
- 긴 주소
- 장바구니 상품 여러 개

## 수동 테스트 시나리오

```text
1. 앱 실행
2. 스플래시에 스피너가 없는지 확인
3. 온보딩 3페이지 이동
4. 시작하기 버튼 노출 조건 확인
5. 이름 질문 대사 페이드 확인
6. 사용자 이름 저장 확인
7. 플랫폼 확인 배너 슬라이드 확인
8. 홈 진입
9. “딸랑구야 도와줘!” 클릭
10. 상품 요청 화면 진입
11. 마이크 상태 변화 확인
12. 상품 추천 문장 줄바꿈 확인
13. 수량 확인 UI 대비 확인
14. 장바구니 진행바 확인
15. 장바구니 목록 영역 확인
16. 배송지 카드 확인
17. 비밀번호 키패드 확인
18. 자동화 배너 확인
19. 쇼핑 완료 화면 확인
20. 홈 복귀 확인
```

---

# 12. 미결정 사항

구현 전에 사용자에게 확인해야 한다.

## 필수 확인

1. 온보딩은 최초 1회만 표시하는가, 매번 표시하는가?
2. 온보딩 `시작하기` 후 본인확인으로 이동하는가, 홈으로 이동하는가?
3. 플랫폼 확인 화면은 본인확인 전인가 후인가?
4. 대화 종료 버튼 최종 스타일은 무엇인가?
   - 흰색 + 회색 테두리 + 진회색 텍스트
   - 검정 배경 + 진한 핑크 텍스트
   - 화면별 variant
5. `52p 템플릿`의 실제 파일 경로는 어디인가?
6. 딸랑구 캐릭터 asset 파일 경로는 어디인가?
7. 실제 음성 인터랙션 route 또는 시작 함수는 무엇인가?
8. 플랫폼 확인에서 구매이력까지 실제로 조회하는가?
9. 자동화 완료 신호는 Flutter에 어떤 방식으로 전달되는가?

## 구현 중 확인 가능

Codex가 저장소 분석으로 확인 가능한 항목은 먼저 조사하고, 찾지 못한 항목만 질문한다.

---

# 13. 권장 작업 순서

## Phase 1. 저장소 분석

- 기존 route
- 기존 컴포넌트
- asset
- 상태 관리
- 음성 진입점
- Accessibility 연결부 확인

## Phase 2. 공통 컴포넌트

- 스테퍼
- 말풍선
- 강조 텍스트
- 마이크 버튼
- 종료 버튼
- 하단 상태 배너
- 공통 CTA

## Phase 3. 앱 진입 흐름

- 스플래시
- 온보딩
- 본인확인
- 플랫폼 확인
- 스몰톡
- 홈

## Phase 4. 핵심 네비게이션

- 홈 CTA → 기존 음성 인터랙션

## Phase 5. 쇼핑 프로세스 수정

- 상품 요청
- 상품 추천
- 수량
- 장바구니 진행
- 장바구니 확인
- 배송지
- 비밀번호

## Phase 6. 자동화 및 완료 UI

- Accessibility 배너
- 완료 화면
- 홈 복귀

## Phase 7. 테스트

- 위젯 테스트
- 수동 플로우
- 작은 화면
- 큰 글자
- 긴 데이터

---

# 14. Codex에게 바로 전달할 시작 프롬프트

아래 프롬프트를 VS Code Codex에 그대로 전달할 수 있다.

```text
현재 Flutter 저장소에서 UI_modplan 작업을 시작하려고 해.

먼저 코드를 수정하지 말고 저장소를 분석해줘.

확인할 항목:
1. 라우팅 방식과 현재 route 목록
2. 홈 화면 또는 메인 화면 파일
3. 기존 음성 인터랙션 진입 화면과 진입 함수
4. 기존 스몰톡/말풍선/마이크 버튼 컴포넌트
5. 사용자 이름이 저장되고 전달되는 방식
6. 딸랑구 캐릭터 asset 경로
7. 공통 색상, 텍스트 스타일, 버튼 스타일
8. Accessibility Service와 Flutter 연결 관련 파일
9. UI_modplan 구현 시 수정할 파일과 신규 파일 후보
10. 기획상 결정이 필요한 항목

분석 결과를 아래 형식으로 정리해줘.

## 저장소 분석 결과
- 라우팅 방식:
- 홈 화면 경로:
- 음성 인터랙션 진입점:
- 스몰톡 화면/템플릿 경로:
- 사용자 이름 상태 위치:
- 캐릭터 asset 경로:
- 공통 색상/텍스트 테마:
- Accessibility 연동 관련 파일:
- 수정할 예상 파일:
- 새로 만들 예상 파일:
- 확인이 필요한 사항:

중요:
- 아직 코드는 수정하지 마.
- 실제 파일과 코드에서 확인한 내용만 말해.
- route, asset, 상태 구조를 추측하지 마.
```

---

# 15. Phase별 Codex 프롬프트

## 15.1 공통 컴포넌트

```text
저장소 분석 결과를 바탕으로 UI_modplan의 공통 컴포넌트를 정리해줘.

우선 구현 후보:
- ShoppingProgressStepper
- DialogueBubble
- 강조 텍스트 구조
- VoiceInputButton
- EndConversationButton
- BottomStatusBanner
- AccessiblePrimaryButton

요구사항:
- 기존 유사 컴포넌트가 있으면 확장하고 중복 생성하지 마.
- 현재 상태 관리와 테마 구조를 유지해.
- 공개 API를 먼저 제안하고, 수정 파일 목록과 영향 범위를 보여줘.
- 대화 종료 버튼 스타일 충돌은 임의로 통일하지 말고 variant 구조로 처리 가능한지 검토해.
- 제안 후 내 확인을 기다리고 아직 코드는 수정하지 마.
```

## 15.2 앱 진입 흐름

```text
공통 컴포넌트를 사용해서 다음 앱 진입 화면을 구현해줘.

- 스플래시 스피너 삭제
- 온보딩 3페이지
- 본인확인/이름 질문
- 플랫폼 확인 하단 배너
- 일반 스몰톡
- 메인 홈

중요:
- 기존 라우팅 방식을 사용해.
- 사용자 이름을 하드코딩하지 마.
- 기존 딸랑구 asset을 사용해.
- 온보딩 표시 정책이 아직 확정되지 않았다면 설정값 또는 TODO로 분리하고 어떤 결정을 내려야 하는지 알려줘.
- 홈의 “딸랑구야 도와줘!” 버튼은 기존 음성 인터랙션 진입점에 실제 연결해.
- 장바구니 보기와 지난 주문 내역은 UI만 구현하고 TODO 콜백으로 남겨.
- 작업 후 변경 파일, 구현 내용, 남은 TODO, 실행 방법을 정리해줘.
```

## 15.3 쇼핑 화면 수정

```text
기존 쇼핑 프로세스 화면을 UI_modplan 기준으로 수정해줘.

대상:
1. 상품 요청 음성 화면
2. 상품 추천 확인
3. 수량 확인
4. 장바구니 담기 진행
5. 장바구니 확인
6. 배송지 확인
7. 비밀번호 입력

공통 원칙:
- 폰트 크기를 줄여서 줄바꿈 문제를 해결하지 마.
- 큰 글씨, 큰 터치 영역, 높은 대비를 유지해.
- 사용자 이름, 상품명, 주소는 동적 값으로 유지해.
- 기존 기능과 상태 로직은 유지하고 UI만 필요한 범위에서 수정해.
- 스테퍼 컴포넌트를 재사용해.
- 기존 마이크 펄스 애니메이션을 유지해.
- 장바구니 스크롤 로직은 유지하고 viewport 높이만 늘려.
- 변경 전후 동작 차이와 수정 파일을 정리해줘.
```

## 15.4 Accessibility 배너와 완료 화면

```text
UI_modplan 기준으로 Accessibility 자동화 시점의 하단 배너와 쇼핑 완료 화면을 구현해줘.

자동화 배너:
- 하단 약 1/4 크기
- 상태 텍스트 외부 String 주입
- 기본 문구 “결제해볼게요!”
- onCancelPressed 콜백
- onAutomationComplete 콜백 인터페이스
- 상단 경계선 또는 그림자
- 0.3~0.5초 ease-in-out 축소/확장 전환

범위 제외:
- 실제 Accessibility Service 자동화
- 실제 시스템 오버레이 윈도우 크기 제어
- 실제 자동화 중단 로직
- 완료 신호 발생 로직

완료 화면:
- 4단계 스테퍼 모두 완료 체크
- “쇼핑 완료!”
- “주문이 성공적으로 완료되었어요.”
- “이제 안심하고 기다리시면 돼요.”
- 원형 핑크 체크 아이콘과 은은한 반짝임
- “홈 화면으로 돌아가기” 버튼
- 기존 홈 route로 이동
- 결제 화면이 back stack에 남지 않도록 기존 라우팅 관례에 맞게 처리

구현 후 콜백 연결 지점과 다른 팀원이 연결해야 할 부분을 명확히 표시해줘.
```

---

# 16. 최종 완료 보고 형식

Codex는 각 Phase 완료 후 다음 형식으로 보고한다.

```md
## 구현 결과

### 변경한 파일
- `path/to/file.dart`
- `path/to/component.dart`

### 신규 파일
- `path/to/new_screen.dart`

### 구현 내용
- ...

### 기존 기능 유지 여부
- ...

### 실행 및 확인 방법
1. ...
2. ...

### 테스트
- 실행한 테스트:
- 통과 결과:
- 수동 확인 항목:

### 남은 TODO
- ...

### 사용자 결정 필요
- ...
```

---

# 17. 최종 승인 체크리스트

## 앱 진입

- [ ] 스플래시 스피너 삭제
- [ ] 온보딩 3페이지
- [ ] 인디케이터
- [ ] 마지막 페이지 시작 버튼
- [ ] 본인확인 대사 전환
- [ ] 강조 단어 동적 처리
- [ ] 플랫폼 확인 배너
- [ ] 일반 스몰톡
- [ ] 홈 화면
- [ ] 홈 CTA 실제 연결

## 쇼핑 프로세스

- [ ] 스테퍼 정렬
- [ ] 이름 질문 두 줄 처리
- [ ] 마이크 상태 색상
- [ ] 예시 칩 2개
- [ ] 상품 추천 문장 줄바꿈
- [ ] 상품 카드 테두리/배경
- [ ] 수량 질문 두 줄
- [ ] 이미지 위 UI 대비
- [ ] 장바구니 진행 텍스트
- [ ] 회색 진행바 트랙
- [ ] 장바구니 목록 영역 확대
- [ ] 배송지 카드 회색 톤
- [ ] 키패드 배경 회색
- [ ] 숫자 확대

## 자동화 및 완료

- [ ] 하단 자동화 배너
- [ ] 외부 상태 텍스트
- [ ] 취소 콜백
- [ ] 완료 콜백
- [ ] 축소/확장 애니메이션
- [ ] 완료 화면
- [ ] 전 단계 완료 체크
- [ ] 홈 복귀
- [ ] back stack 정리

## 접근성

- [ ] 큰 글씨 유지
- [ ] 충분한 명암 대비
- [ ] 큰 터치 영역
- [ ] 긴 이름 대응
- [ ] 긴 주소 대응
- [ ] 작은 화면 대응
- [ ] 시스템 글자 크기 확대 대응

---

# 18. 문서 외 제안과 원문 요구의 구분

아래 내용은 구현 편의를 위한 제안이며 PDF에서 특정 구현 기술로 확정한 내용은 아니다.

- `SharedPreferences`를 통한 온보딩 완료 저장
- `AnimatedSwitcher`를 통한 대사 페이드
- enum 기반 스테퍼 상태
- `EndConversationButtonVariant`
- `AutomationUiState`
- route stack 정리를 위한 `pushReplacement`, `go`, `popUntil`

Codex는 프로젝트의 기존 구조와 라이브러리를 우선하며, 위 기술을 반드시 새로 도입할 필요는 없다.
