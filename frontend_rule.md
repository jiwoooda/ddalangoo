# 프론트엔드 규칙

1. 대화 중인 모든 화면에서는 말하기 버튼이 하단에 존재해야 한다.
2. 말하기 버튼은 딸랑구와 사용자의 대화 턴에 따라 비활성화/활성화 되어야 한다. 
3. 사용자는 대화 중 언제든 대화 종료를 할 수 있으며, 해당 시점까지 담긴 장바구니 상품은 저장되어야 한다. 
4. 이 상품들은 홈 화면의 "장바구니 바로가기" 탭을 누르면 볼 수 있어야 한다.
5. live 쇼핑 플로우의 말풍선 텍스트 원문은 AI/백엔드가 내려주는 값을 우선 사용한다.
6. 다만 mock 화면, fallback 상태, 초기 진입 안내 문구는 프론트가 임시 텍스트를 가질 수 있다.
7. 프론트는 말풍선의 어투를 임의로 바꾸지 않고, 화면 표시 순서/강조/애니메이션만 제어한다. 즉 어투나 말투 튜닝은 AI 쪽에서 담당한다.
8. 모든 화면은 `ScreenFrame`, `AppLayout`, `AppResponsive` 같은 공통 레이아웃 계층을 우선 사용한다.
9. 화면 파일 안에서 여백, 높이, 폰트 크기를 직접 하드코딩하지 않고, 공통 token 또는 반응형 helper를 우선 사용한다.
10. 작은 화면에서는 overflow를 허용하지 않고, 필요하면 스크롤로 안전하게 내려가야 한다.
11. 넓은 화면에서는 콘텐츠가 좌우로 과하게 늘어나지 않도록 최대 폭 제한을 유지한다.

