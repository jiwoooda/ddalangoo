"""
Stage4 스코어링 프롬프트 — 초안. 세부 문구는 추후 별도 조정 예정.

판단(LLM)과 계산(코드)의 경계: axis_weight 판단 + soft_preferences 항목별
categorical 매칭 판정만 LLM이 하고, 정규화/카테고리→숫자 매핑/항목 평균/
가중합은 이미 정답이 있는 산술이라 aggregator.py(코드)가 결정론적으로
계산한다.
"""

SCORING_PROMPT = """\
당신은 쇼핑 어시스턴트의 랭킹 판단기입니다. 아래 후보 전체를 동시에 놓고
두 가지를 판단하세요.

[공통 원칙 — 우선순위 체계]
아래 두 판단(axis_weights, preference_item_scores) 모두에 다음 우선순위를
동일하게 적용하세요: 안전 제약 > 이번 요청(Intent)에서 명시된 조건 >
명시적으로 배제된 것 > 애매하고 뉘앙스性인 선호.
(안전 제약/명시적 배제는 이미 이전 단계에서 처리 완료되어 이 단계
후보에는 반영되어 있으므로, 여기서는 주로 "이번 요청에서 명시된 조건"이
"애매한 선호"보다 항상 우선한다는 원칙으로 적용됩니다.)

[1. axis_weights]
이번 쿼리와 컨텍스트를 보고 price/review/preference 세 축의 중요도를
0~1 사이 weight로 판단하세요. 세 개 다 반드시 포함하세요 (weight가
낮더라도 항목 자체는 빠뜨리지 마세요). 각 weight에 근거를 반드시
남기세요.
예) 쿼리에 "제일 싼거"가 명시되면(Intent 명시 조건) price weight를
    다른 축보다 높게 책정하고 그 이유를 적으세요 — 이는 위 우선순위
    원칙("이번 요청에서 명시된 조건 > 애매한 선호")의 적용 사례입니다.

[2. preference_item_scores]
아래 "선호 컨텍스트" 목록에 있는 항목 하나하나에 대해, 후보 전체
각각과 얼마나 부합하는지 개별적으로 판정하세요. 즉 (선호 항목 수 ×
후보 수)만큼의 PreferenceItemScore를 만드는 것이 원칙입니다. 항목들을
뭉쳐서 후보당 하나로만 판정하지 마세요 — 서로 다른 방향을 가리키는
항목(예: "저가"와 "프리미엄"이 동시에 감지된 경우)의 근거가 뭉개집니다.

각 (항목, 후보) 쌍에 대해 강한부합/부합/중립/배치 중 하나로 판정하고
reasoning을 남기세요.

규칙:
- 후보 하나씩 따로 호출하지 말고 이 한 번의 응답 안에서 후보 전체를
  동시에 비교하며 판정할 것 (상대적 비교가 안정적입니다)
- tier1(안전제약)/tier2(명시적 배제)는 이미 이전 단계에서 배제된 후보만
  여기 들어오므로 여기서 다시 판단하지 마세요
- 선호 항목이 약하거나 모호해서 구분이 어려운 경우에도 비워두지 말고
  "중립"으로 채우세요 — 이 항목들에 대해 빈 리스트를 반환하는 것은
  허용되지 않습니다
- candidate_id는 아래 후보 목록의 대괄호 레이블([A], [B]...)을 그대로
  쓰세요
- preference_item에는 아래 "선호 컨텍스트" 목록 각 줄의 핵심 값만 쓰세요
  (번호와 괄호 안 출처/구매일 메타정보는 제외)
- "선호 컨텍스트" 목록은 이미 최근 순으로 정렬되어 있습니다 (번호가
  빠를수록 더 최근 — 구매이력 항목은 실제 구매일 기준, 그 외는 세션
  발화가 프로필/과거 이력보다 항상 최근으로 취급됨). 항목들이 서로 다른
  방향을 가리켜 충돌하면, 위 우선순위 원칙으로도 안 갈리는 경우에 한해
  목록에서 더 앞(더 최근)에 있는 항목을 우선하세요

[conflict_note — 최상위 1개, 선택]
선호 항목들끼리 서로 충돌하는 방향을 가리켜서(예: "저가"와 "프리미엄"이
동시에 감지됨) 위 우선순위 원칙 또는 recency로 판단을 내린 경우에만,
그 근거를 전체를 아우르는 conflict_note 한 줄로 반드시 남기세요. 충돌이
없으면 conflict_note는 비워두세요 (null).

# 이번 요청 키워드
{keywords}

# 정렬 조건 (사용자가 명시한 경우)
{condition}

# 선호 컨텍스트 (tier3 — soft_preferences, 배제 아님 랭킹 가중치용)
{soft_preferences}

# 후보 상품
{formatted_candidates}
"""
