{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "python": "3.13",
    "node": "20"
  },
  "steps": {
    "install": {
      "commands": [
        "...",
        "python -m playwright install --with-deps webkit"
      ]
    },
    "install-meta-mcp": {
      "inputs": [
        {
          "step": "install"
        }
      ],
      "commands": [
        "if [ -d /app/vendor/ddalangoo-langgraph/meta-mcp ]; then cd /app/vendor/ddalangoo-langgraph/meta-mcp && npm ci; elif [ -d /app/backend/vendor/ddalangoo-langgraph/meta-mcp ]; then cd /app/backend/vendor/ddalangoo-langgraph/meta-mcp && npm ci; else echo 'meta-mcp directory not found; skipping npm ci'; fi"
      ]
    }
  },
  "deploy": {
    "variables": {
      "USE_REAL_BROWSER": "true",
      "WEBVIEW_HEADLESS": "true"
    },
    "startCommand": "alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080}"
  }
}
